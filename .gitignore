# Internal-only working notes and Claude Code session state — not meant for a
# public repo. docs-reference/ in particular held a full internal security
# review of the LIVE production platforms (foundation/foundation-mobile),
# removed 2026-09-02 after this repo went public.
docs-reference/
HANDOFF.md
.claude/
CLAUDE.md

# .github/workflows/ removed 2026-09-02: both workflows targeted the old
# pre-fork ios/FoundationMobile.xcodeproj (gone since Task A2 replaced ios/
# with Rarimo's real source) and referenced live App Store Connect /
# distribution-cert secrets for the LIVE foundation-mobile app - stale and
# actively dangerous to leave dispatchable. A fresh CI setup for the actual
# Rarimo-fork build belongs in a later task, not resurrected from this.

# Firebase — contain live API keys; injected at build time from CI secret / local keychain
ios/FoundationMobile/GoogleService-Info.plist
ios/FoundationMobile/GoogleService-Info-staging.plist
# Task A2 quarantined the old ios/ tree to legacy-ios-shell/ (git mv) without
# updating these paths — the files above are now dead entries and these two
# are what's actually needed. Kept both: the old paths are harmless if the
# B-track port later recreates a similarly-named target under the new ios/.
legacy-ios-shell/FoundationMobile/GoogleService-Info.plist
legacy-ios-shell/FoundationMobile/GoogleService-Info-staging.plist
# Task A2's subtree import brought in Rarimo's own GoogleService-Info.plist
# at ios/Rarime/GoogleService-Info.plist, tracked (it's their own already-
# public Firebase config, not a leak) - untracked pre-emptively there
# because a future B-track task would overwrite its content with
# Foundation's real foundation-next-app credentials. Task B2 renamed
# ios/Rarime/ -> ios/FoundationMobile/, so that path is now covered by
# line 18 above instead - no separate entry needed.

# OSX
#
.DS_Store

# Xcode
#
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
**/.xcode.env.local

# Android/IntelliJ
#
build/
.idea
.gradle
local.properties
*.iml
*.hprof
.cxx/
*.keystore
!debug.keystore
.kotlin/

# node.js
#
node_modules/
npm-debug.log
yarn-error.log

# fastlane
#
# It is recommended to not store the screenshots in the git repo. Instead, use fastlane to re-generate the
# screenshots whenever they are needed.
# For more information about the recommended setup visit:
# https://docs.fastlane.tools/best-practices/source-control/

**/fastlane/report.xml
**/fastlane/Preview.html
**/fastlane/screenshots
**/fastlane/test_output

# Bundle artifact
*.jsbundle

# Ruby / CocoaPods
**/Pods/
/vendor/bundle/

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# testing
/coverage

# Yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.gstack/
.worktrees/
