# Upstream rarime-android-app declared `*.aar filter=lfs` here to route .aar
# binaries through Git LFS. Per this repo's plan (Open Decision OD-7,
# resolved: "commit binaries plainly, no Git LFS"), that filter is stripped
# intentionally and permanently — not just because no .aar is currently
# tracked. Do not reintroduce an `lfs` filter line here; a future upstream
# merge or an added .aar file should commit plainly like everything else in
# this repo.
